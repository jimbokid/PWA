import { API_PATH } from './appConstants';

export const DISCOVER_POPULAR = `${API_PATH}discover/movie`;
export const SEARCH = `${API_PATH}search/multi`;

export const SEARCH_MOVIE = `${API_PATH}search/movie`;
export const SEARCH_TV = `${API_PATH}search/tv`;
export const SEARCH_PERSON = `${API_PATH}search/person`;

